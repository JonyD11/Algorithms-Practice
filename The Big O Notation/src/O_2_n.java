
public class O_2_n {

	/*
	 * What is O(2 ^ n)?
	 * O(2 ^ n) denotes an algorithm whose doubles with each 
	 * addition to the input data set. The growth curve of an 
	 * O(2 ^ n) function is exponential - starting off very 
	 * shallow, then rising meteorically.
	 * O(2 ^ n) is directly proportional to O(log n).
	 * 
	 * */
}
